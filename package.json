{
    "name": "darell",
    "workspaces": [
      "frontend",
      "backend"
    ],
    "scripts": {
        "build": "cd ./backend && npm install && npm run build && npm run db:sync && cd ../frontend && npm install && npm run build"
    }
  }
  