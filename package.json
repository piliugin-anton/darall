{
    "name": "darell",
    "workspaces": [
      "frontend",
      "backend"
    ],
    "scripts": {
        "build": "cd ./backend && npm install && npm run db:sync && npm run build && cd ../frontend && npm install && npm run build"
    }
  }
  